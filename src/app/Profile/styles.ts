import {colors} from 'src/lib';
import GlobalStyle from 'lib/globalStyle';
import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  button: {marginTop: 10},
  card: {
    marginTop: 50,
    alignItems: 'center',
  },
  btnWrap: {width: '100%', paddingHorizontal: 5},
});
