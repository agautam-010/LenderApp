export enum BUTTON_TYPES {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  DISABLED = 'disabled',
}

export enum SPINNER_TYPE {
  LOADING = 'loading',
  SUCCESS = 'success',
  FAILURE = 'failure',
}

export enum TOAST_TYPES {
  SUCCESS = 'success',
  ERROR = 'error',
  INFO = 'info',
}

export enum THEME_TYPES {
  DEFAULT = 'default',
  LIGHT = 'light',
  DARK = 'dark',
  GREEN = 'green',
}

export enum DIVIDER_ALIGN_TYPE {
  INSET = 'inset',
  MIDDLE = 'middle',
  FULL_BLEED = 'full-bleed',
  BLANK = '',
}
